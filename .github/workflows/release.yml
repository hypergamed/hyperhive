name: Release

on:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  # Turborepo requires these exact env var names for remote caching
  TURBO_TOKEN: ${{ secrets.VERCEL_ORG_TOKEN }}
  TURBO_TEAM: ${{ vars.VERCEL_ORG_TEAM }}

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      id-token: write

    steps:
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ vars.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}

      - name: Checkout
        uses: actions/checkout@v6
        with:
          token: ${{ steps.app-token.outputs.token }}
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: "pnpm"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Test
        run: pnpm test

      # =============================================================================
      # NPM PUBLISHING DISABLED
      # =============================================================================
      # Publishing to npm is temporarily disabled. To re-enable:
      # 1. Uncomment the "Create Release PR or Publish" and "Published packages" steps below
      # 2. Remove or comment out the "Publishing Disabled Notice" step
      # =============================================================================

      - name: Publishing Disabled Notice
        run: |
          echo "============================================================================="
          echo "ℹ️  NPM PUBLISHING IS CURRENTLY DISABLED"
          echo "============================================================================="
          echo ""
          echo "Build and tests have passed successfully."
          echo "Package publishing to npm is temporarily disabled."
          echo ""
          echo "To re-enable publishing, update .github/workflows/release.yml"
          echo "============================================================================="

      # - name: Create Release PR or Publish
      #   id: changesets
      #   uses: changesets/action@v1
      #   with:
      #     version: pnpm version-packages
      #     publish: pnpm release
      #     commit: "chore(release): version packages"
      #     title: "chore(release): version packages"
      #   env:
      #     GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
      #     NPM_CONFIG_PROVENANCE: true

      # - name: Published packages
      #   if: steps.changesets.outputs.published == 'true'
      #   run: |
      #     echo "Published packages:"
      #     echo '${{ steps.changesets.outputs.publishedPackages }}' | jq .
